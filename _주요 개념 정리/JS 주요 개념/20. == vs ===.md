# 20. == vs ===

: == (비교연산자) , === (엄격한 비교연산자)

: `===`는 **타입 변환이 일어나지 않으며 타입이 일치해야한다**

```
'' == '0'           // false
0 == ''             // true
0 == '0'            // true
```

: `==` 는 **타입 변환이 발생한다**

:  **단순 타입 체크와 더 좋은 코드를 위해 엄격한 동등 비교 연산자(===)를 사용하는 것이 바람직하다**